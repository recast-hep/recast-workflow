Catalogue Query
===============

Common parameters can optionally be specified with additional data. 
Using the analysis example, a theorist might want to only use tools that have validated their implementation of the analysis.