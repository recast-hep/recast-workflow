description: 'Perform event selection on hepmc files using rivet. The yoda output is converted into generic json format for statistical analysis.'
settings:
  - {name: rivet_version, default: latest}
inputs:
interfaces:
  input: hepmc
  output: json
  